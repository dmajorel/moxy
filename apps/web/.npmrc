# Refuse to install on a Node the dependencies do not support, rather than
# succeed and fail later inside jsdom with something unreadable. The range is
# in package.json's "engines"; this is what makes npm honour it.
engine-strict=true
