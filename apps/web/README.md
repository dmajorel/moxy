# apps/web

Frontend de moxy. **Pas encore échafaudé** : il arrive à l'étape 3 du §6 de
[`docs/PROXMOX_UI_HANDOFF.md`](../../docs/PROXMOX_UI_HANDOFF.md), une fois que le
backend agrégateur expose `/api/overview`.

Cible : React + Tailwind + Tabler Icons, avec les tokens de thème du §2 du document
de passation (`--surface-0/1/2`, `--border`, `--text-*`, `--radius`, `--font-mono`).
Les quatre maquettes de référence sont en annexe A.
