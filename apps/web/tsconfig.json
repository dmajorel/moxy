{
  // A solution file: it compiles nothing itself and only points at the two
  // programs below. They are kept apart because they run in different places
  // — one in a browser, one in Node — and a single `types` list gave the
  // application code `process` and `Buffer`, which it must never reach for.
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
